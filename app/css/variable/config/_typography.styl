//  ============================ Typography ============================

//webfont('RUB', 'rouble-webfont', 'normal')
//Add fotn rub write .rub a - (a,b,c,d - change type font)

$v-font =  'Montserrat', sans-serif

typeSize(fontSm,font)
	+below(768px)
		font-size font px
	font-size fontSm px

/*
font-size 14px
@extend .type--sm

font-size 20px
@extend .type--main

font-size 24px
@extend .type--md

font-size 30px
@extend .type--lg

font-size 40px
@extend .type--xl
*/

.type--xs
	typeSize(12,10)

.type--sm
	typeSize(14,12)

.type--main
	typeSize(16,14)

.type--md
	typeSize(24,16)

.type--lg
	typeSize(30,18)

.type--xl
	typeSize(36,20)

.type--bold
	font-weight bold
	font-family: Fira Sans

h1
	@extend .type--xl
	@extend .type--bold
h2
	@extend .type--lg
	@extend .type--bold
h3
	@extend .type--md
	@extend .type--bold
h4
	@extend .type--main
	@extend .type--bold
h5
	@extend .type--sm
	@extend .type--bold

a
	color $link-color
	text-decoration: none
	&:hover
		color darken($link-color,20)
	&:visiter
		color darken($link-color,30)

p
	@extend .type--main

.link--mark
	color $pink-mark
	&:hover
		color darken($pink-mark,20)
	&:visiter
		color darken($pink-mark,30)

.link--main
	color $link-color
	hover-underline($link-color)
	&:hover
		color darken($link-color,20)
	&:visiter
		color darken($link-color,30)

.link--text
	color $pink-mark
	text-decoration underline
	&:hover
		text-decoration none
